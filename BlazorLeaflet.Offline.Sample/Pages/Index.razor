@page "/"
@inject HttpClient Http
<div>@Message</div>

@*<button @onclick="TestClick">Click here to test</button>
<button @onclick="ShowTiles">Click here to show Tile List</button>
*@
   <div class="container-fluid" style="max-width:1200px">
      <div class="row">
        <div class="col-md-12">
          <h1>Leaflet.offline example</h1>
@*          <p>
            Progress: <span id="progress">0</span> / <span id="total">0</span>
            Current storage length:
            <span id="storage"></span> files
            <button
              class="btn btn-success"
              id="show_storage"
              data-toggle="modal"
              data-target="#storageModal"
            >
              Show storage info
            </button>
            <button class="btn btn-danger" id="remove_tiles">
              <i
                class="fa fa-trash"
                aria-hidden="true"
                title="Remove tiles"
              ></i>
            </button>
          </p>
*@        </div>
        <div class="col-md-12">
          <div id="map" style="height: 75vh"></div>
        </div>
      </div>
    </div>
    <div
      class="modal fade"
      tabindex="-1"
      role="dialog"
      aria-labelledby="storageModal"
      aria-hidden="true"
      id="storageModal"
    >
      <div class="modal-dialog modal-xl modal-dialog-scrollable ">
        <div class="modal-content">
          <div class="modal-body">
            <table class="table table-striped table-sm">
              <thead>
                <tr>
                  <th></th>
                  <th>Source url</th>
                  <th>Storage key</th>
                  <th>createdAt</th>
                </tr>
              </thead>
              <tbody id="tileinforows"></tbody>
            </table>
          </div>
        </div>
      </div>
    </div>


<style>
    .custom-toolbar {
        width: 100%;
        background-color: #f6f6f6;
        height: 40px;
        line-height: 10px;
        border-bottom: inset;
        border-bottom-width: 1px;
        padding: 17px 15px;
        color: #656565;
    }

    .custom-toolbar .mail-box {
        font-size: 20px;
    }

    .inbox-container {
        height: 500px;
        margin: 30px auto;
        width: 70%;
    }

    .k-card {
        padding: 6px 20px;
        width: 127px;
        font-size: 18px;
        line-height:26px;
    }

    .k-card .k-icon {
        font-size: 18px;
        margin-top: 3px;
        margin-right: 4px;
    }
</style>